{
  "name": "talkyhub-web-widget",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "TalkyHub embeddable live-chat widget (Preact + TypeScript).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@preact/signals": "^1.3.2",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "~5.7.3",
    "vite": "^6.3.5"
  }
}
